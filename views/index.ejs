<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="output.css">
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Add AOS Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
</head>
<body class="bg-gradient-to-br from-slate-900 via-indigo-900/90 to-slate-900 overflow-x-hidden">
    <!-- Modern Navbar -->
    <nav class="sticky top-0 z-50 bg-slate-900/50 backdrop-blur-xl border-b border-indigo-500/10">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex items-center justify-between h-16">
                <!-- Logo -->
                <div class="flex-shrink-0 flex items-center gap-2">
                    <a href="/" class="text-white font-bold text-lg sm:text-xl bg-gradient-to-r from-indigo-400 to-blue-400 bg-clip-text text-transparent animate-shine">YCIS | DATA CENTER</a>
                </div>
                
                <!-- Desktop Menu -->
                <div class="hidden md:block">
                    <div class="ml-10 flex items-center space-x-8">
                        <a href="/" class="text-white/70 hover:text-white transition-colors">Home</a>
                        <a href="/web-hosting" class="text-white/70 hover:text-white transition-colors">Web Hosting</a>
                        <a href="/vps-hosting" class="text-white/70 hover:text-white transition-colors">VPS Hosting</a>

                        <a href="/contact" class="text-white/70 hover:text-white transition-colors">Contact</a>
                    </div>
                </div>

                <!-- Mobile menu button -->
                <div class="md:hidden">
                    <button type="button" class="text-white hover:text-white/70 transition-colors" id="mobile-menu-button">
                        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Mobile Menu -->
            <div class="hidden md:hidden" id="mobile-menu">
                <div class="px-2 pt-2 pb-3 space-y-1 bg-slate-800/95 backdrop-blur-xl rounded-lg mt-2">
                    <a href="/" class="block px-3 py-2 text-white/70 hover:text-white hover:bg-indigo-500/10 rounded-lg">Home</a>
                    <a href="/web-hosting" class="block px-3 py-2 text-white/70 hover:text-white hover:bg-indigo-500/10 rounded-lg">Web Hosting</a>
                    <a href="/vps-hosting" class="block px-3 py-2 text-white/70 hover:text-white hover:bg-indigo-500/10 rounded-lg">VPS Hosting</a>
                    <div class="px-3 py-2">
                        <span class="block text-white/70 mb-2">Services</span>
                        <div class="pl-4 space-y-1">
                            <a href="/security" class="block px-3 py-2 text-white/60 hover:text-white hover:bg-indigo-500/10 rounded-lg">Security</a>
                            <a href="/cloud-storage" class="block px-3 py-2 text-white/60 hover:text-white hover:bg-indigo-500/10 rounded-lg">Cloud Storage</a>
                            <a href="/backup" class="block px-3 py-2 text-white/60 hover:text-white hover:bg-indigo-500/10 rounded-lg">Backup Solutions</a>
                        </div>
                    </div>
                    <a href="/contact" class="block px-3 py-2 text-white/70 hover:text-white hover:bg-indigo-500/10 rounded-lg">Contact</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <section class="relative min-h-[calc(100vh-4rem)] flex items-center py-12 sm:py-24 overflow-hidden">
        <!-- Video Background -->
        <div class="absolute inset-0 w-full h-full">
            <video 
                class="w-full h-full object-cover"
                autoplay 
                muted 
                loop 
                playsinline
                preload="auto"
                poster="/images/Server.png"
                id="bgVideo"
            >
                <source src="/videos/datacenter.mp4" type="video/mp4" />
                <source src="/videos/datacenter.webm" type="video/webm" />
                Your browser does not support the video tag.
            </video>
            <!-- Enhanced Overlay with Multiple Layers -->
            <div class="absolute inset-0 bg-gradient-to-br from-black/90 to-black/70 to-purple-900/50"></div>
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,transparent_0%,rgba(0,0,0,0.7)_100%)]"></div>
            <div class="absolute inset-0 backdrop-blur-[1px]"></div>
        </div>

        <!-- Add this script at the end of your body tag -->
        <script>
            document.addEventListener('DOMContentLoaded', function() {
                const video = document.getElementById('bgVideo');
                video.play().catch(function(error) {
                    console.log("Video autoplay failed:", error);
                });
            });
        </script>
        <!-- Content Container -->
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <!-- Left Content -->
                <div class="space-y-8 animate-reveal [animation-delay:0.2s]">
                    <!-- Welcome Badge -->
                    <div class="inline-block animate-float">
                        <div class="px-4 py-2 rounded-full bg-white/5 backdrop-blur-sm border border-white/10 hover:border-purple-500/50 transition-colors">
                            <span class="text-sm font-medium text-white/80">Welcome to YCIS Data Center</span>
                        </div>
                    </div>

                    <!-- Main Title with Enhanced Animation -->
                    <h1 class="text-5xl sm:text-6xl lg:text-7xl font-bold leading-tight text-white">
                        <span class="block mb-2 animate-slide-up [animation-delay:0.3s]">Next-Gen</span>
                        <span class="block animate-slide-up [animation-delay:0.4s]">
                            Data Center
                        </span>
                        <span class="block animate-slide-up [animation-delay:0.5s]">Solutions</span>
                    </h1>

                    <!-- Enhanced Description -->
                    <p class="text-xl sm:text-2xl text-white/70 max-w-2xl animate-reveal [animation-delay:0.6s]">
                        Empowering businesses with cutting-edge infrastructure, advanced security, and seamless scalability for the digital age.
                    </p>

                    <!-- Enhanced CTA Buttons -->
                    <div class="flex flex-col sm:flex-row items-center gap-6 animate-reveal [animation-delay:0.7s]">
                        <button onclick="scrollToServices()" class="group relative px-8 py-4 w-full sm:w-auto overflow-hidden rounded-lg">
                            <div class="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-600 animate-pulse-slow"></div>
                            <div class="absolute inset-0 bg-gradient-to-r from-purple-600 to-pink-600 opacity-0 group-hover:opacity-60 blur-xl transition-opacity duration-500"></div>
                            <div class="relative flex items-center justify-center gap-3">
                                <span class="text-white font-medium text-lg">Data Center Services</span>
                                <svg class="w-6 h-6 text-white transform group-hover:translate-x-2 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                                </svg>
                            </div>
                        </button>
                        
                        <button onclick="window.location.href='/security'" class="group relative px-8 py-4 w-full sm:w-auto overflow-hidden rounded-lg">
                            <div class="absolute inset-0 bg-white/10 backdrop-blur-sm border border-white/20 group-hover:border-purple-500/50 transition-colors duration-300"></div>
                            <div class="relative flex items-center justify-center gap-3">
                                <span class="text-white font-medium text-lg">Explore Features</span>
                                <svg class="w-6 h-6 text-white transform group-hover:translate-x-2 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                                </svg>
                            </div>
                        </button>
                    </div>
                </div>

                <!-- Right Content - Decorative Elements -->
                <div class="hidden lg:block relative animate-reveal [animation-delay:0.8s]">
                    <div class="relative w-full h-[500px]">
                        <!-- Floating Elements -->
                        <div class="absolute top-1/4 right-1/4 w-32 h-32 bg-purple-500/20 rounded-full blur-3xl animate-float"></div>
                        <div class="absolute bottom-1/4 left-1/4 w-40 h-40 bg-pink-500/20 rounded-full blur-3xl animate-float [animation-delay:2s]"></div>
                        <div class="absolute top-1/2 left-1/2 w-36 h-36 bg-violet-500/20 rounded-full blur-3xl animate-float [animation-delay:4s]"></div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- YCIS Section -->
    <section class="relative py-24 overflow-hidden">
        <!-- Background Gradient -->
        <div class="absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-br from-violet-600/10 via-transparent to-cyan-600/10 opacity-50"></div>
        </div>

        <!-- Content Container -->
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <!-- Left Column - Photo -->
                <div class="relative animate-reveal [animation-delay:0.2s]">
                    <div class="aspect-[4/3] rounded-2xl overflow-hidden">
                        <!-- Glass Effect Container -->
                        <div class="absolute inset-0 bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-sm rounded-2xl border border-white/10"></div>
                        
                        <!-- Image Container with Animation -->
                        <div class="relative h-full group">
                            <img 
                                src="/images/YC.jpg" 
                                alt="YCIS Satara" 
                                class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700"
                            />
                            
                            <!-- Gradient Overlay -->
                            <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
                            
                            <!-- Caption -->
                            <div class="absolute bottom-0 left-0 right-0 p-6">
                                <h3 class="text-white text-lg font-semibold mb-2 text-center">YASHAVANTRAO CHAVAN INSTITUTE OF SCIENCE</h3>
                            </div>
                        </div>
                    </div>

                    <!-- Decorative Elements -->
                    <div class="absolute -top-4 -right-4 w-24 h-24 bg-gradient-to-br from-violet-500/30 to-transparent rounded-full blur-2xl"></div>
                    <div class="absolute -bottom-4 -left-4 w-24 h-24 bg-gradient-to-br from-cyan-500/30 to-transparent rounded-full blur-2xl"></div>
                </div>

                <!-- Right Column - Description -->
                <div class="space-y-8 animate-reveal [animation-delay:0.4s]">
                    <!-- Section Title -->
                    <div class="space-y-4">
                        <h2 class="text-3xl sm:text-4xl font-bold">
                            <span class="bg-gradient-to-r from-violet-300 via-cyan-300 to-violet-300 bg-[length:200%_auto] animate-gradient bg-clip-text text-transparent">
                                Yashavantrao Chavan Institute of Science
                            </span>
                        </h2>
                        <div class="w-20 h-1.5 bg-gradient-to-r from-violet-500 to-cyan-500 rounded-full"></div>
                    </div>

                    <!-- Description Text -->
                    <div class="space-y-6 text-white/60">
                        <p class="text-lg leading-relaxed">
                            Yashavantrao Chavan Institute of Science (YCIS), Satara, established in 1965, is a distinguished autonomous science college affiliated with Shivaji University, Kolhapur. Founded under the aegis of Rayat Shikshan Sanstha, a renowned educational trust initiated by Padmbhushan Dr. Karmaveer Bhaurao Patil, the institute has a rich legacy of imparting quality science education.
                        </p>
                    </div>

                    <!-- CTA Button -->
                    <button onclick="window.location.href='https://www.ycis.ac.in/index.php'" class="group relative px-6 py-3">
                        <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-cyan-600 rounded-lg"></div>
                        <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-cyan-600 rounded-lg blur-lg group-hover:opacity-60 transition-opacity duration-500"></div>
                        <div class="relative flex items-center justify-center gap-2">
                            <span class="text-white font-medium">Visit Official Website</span>
                            <svg class="w-5 h-5 text-white transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                            </svg>
                        </div>
                    </button>
                </div>
            </div>
        </div>
    </section>

    <!-- Director's Message Section -->
    <section class="relative py-16">
        <!-- Background Elements -->
        <div class="absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-br from-violet-600/10 via-transparent to-cyan-600/10 opacity-50"></div>
        </div>

        <!-- Content Container -->
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="bg-white/5 backdrop-blur-lg rounded-2xl border border-white/10 p-8">
                <h2 class="text-3xl font-bold text-center mb-8 bg-gradient-to-r from-violet-300 via-cyan-300 to-violet-300 bg-clip-text text-transparent">
                    DIRECTOR'S MESSAGE
                </h2>
                
                <div class="flex flex-col md:flex-row gap-8 items-start">
                    <!-- Director's Image -->
                    <div class="flex-shrink-0 mx-auto md:mx-0">
                        <div class="relative w-32 h-32 rounded-full overflow-hidden border-4 border-gradient-to-r from-violet-500 to-cyan-500">
                            <div class="absolute inset-0 bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-sm"></div>
                            <img 
                                src="/images/image.png" 
                                alt="Director" 
                                class="w-full h-full object-cover object-center transform hover:scale-105 transition-transform duration-300"
                            />
                        </div>
                        <p class="text-center mt-2 text-white/80 font-semibold">Prin. Dr.B.T. Jadhav</p>
                    </div>

                    <!-- Message Content -->
                    <div class="flex-grow">
                        <p class="text-white/70 leading-relaxed">
                            It gives me immense pleasure to welcome you all to the state-of-the-art Data Center at Yashavantrao Chavan Institute of Science. In today's fast-evolving digital landscape, data has become the backbone of innovation, research, and development. Our Data Center stands as a testament to our commitment to technological excellence, security, and efficiency.<br>
                                        This facility is designed to support cutting-edge research, big data analytics, and cloud computing while ensuring data integrity and security. We strive to empower students, researchers, and professionals with world-class infrastructure to foster innovation in fields like Artificial Intelligence, Machine Learning, Cybersecurity, and beyond.<br>
                        </p>
                    </div>
                </div>

        </div>
    </div>
</section>

    <!-- Data Center Section -->
    <section id="services-section" class="relative py-24 overflow-hidden">
        <!-- Background Gradient -->
        <div class="absolute inset-0">
            <div class="absolute inset-0 bg-gradient-to-br from-cyan-600/10 via-transparent to-violet-600/10 opacity-50"></div>
        </div>

        <!-- Content Container -->
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="grid lg:grid-cols-2 gap-12 items-center">
                <!-- Left Column - Description -->
                <div class="space-y-8 animate-reveal [animation-delay:0.2s] order-2 lg:order-1">
                    <!-- Section Title -->
                    <div class="space-y-4">
                        <h2 class="text-3xl sm:text-4xl font-bold">
                            <span class="bg-gradient-to-r from-cyan-300 via-violet-300 to-cyan-300 bg-[length:200%_auto] animate-gradient bg-clip-text text-transparent">
                                Data Center Services
                            </span>
                        </h2>
                        <div class="w-20 h-1.5 bg-gradient-to-r from-cyan-500 to-violet-500 rounded-full"></div>
                    </div>

                    <!-- Description Text -->
                    <div class="space-y-6 text-white/60">
                        <p class="text-lg leading-relaxed">
                            Data Center Services provide infrastructure, management, and security solutions for storing, processing, and managing large volumes of data. These services ensure high availability, scalability, and security for businesses, supporting cloud computing, networking, and enterprise applications.
                        </p>
                    </div>

                    <!-- CTA Button -->
                    <button onclick="window.location.href='/security'" class="group relative px-6 py-3">
                        <div class="absolute inset-0 bg-gradient-to-r from-cyan-600 to-violet-600 rounded-lg"></div>
                        <div class="absolute inset-0 bg-gradient-to-r from-cyan-600 to-violet-600 rounded-lg blur-lg group-hover:opacity-60 transition-opacity"></div>
                        <div class="relative flex items-center justify-center gap-2">
                            <span class="text-white font-medium">View Security Features</span>
                            <svg class="w-5 h-5 text-white transform group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"/>
                            </svg>
                        </div>
                    </button>
                </div>

                <!-- Right Column - Photo -->
                <div class="relative animate-reveal [animation-delay:0.4s] order-1 lg:order-2">
                    <div class="aspect-[4/3] rounded-2xl overflow-hidden">
                        <!-- Glass Effect Container -->
                        <div class="absolute inset-0 bg-gradient-to-br from-white/10 to-white/5 backdrop-blur-sm rounded-2xl border border-white/10"></div>
                        
                        <!-- Image Container with Animation -->
                        <div class="relative h-full group">
                            <!-- Placeholder Image - Replace src with your actual image -->
                            <img 
                                src="/images/Server.png" 
                                alt="Data Center" 
                                class="w-full h-full object-cover transform group-hover:scale-105 transition-transform duration-700"
                            />
                            
                            <!-- Gradient Overlay -->
                            <div class="absolute inset-0 bg-gradient-to-t from-black/50 via-transparent to-transparent"></div>
                            
                            <!-- Caption -->
                            <div class="absolute bottom-0 left-0 right-0 p-6">
                                <h3 class="text-white text-lg font-semibold mb-2 text-center">DATA CENTER</h3>
                            </div>
                        </div>
                    </div>

                    <!-- Decorative Elements -->
                    <div class="absolute -top-4 -right-4 w-24 h-24 bg-gradient-to-br from-cyan-500/30 to-transparent rounded-full blur-2xl"></div>
                    <div class="absolute -bottom-4 -left-4 w-24 h-24 bg-gradient-to-br from-violet-500/30 to-transparent rounded-full blur-2xl"></div>
                </div>
            </div>
        </div>
    </section>

  

    <!-- Footer -->
    <footer class="relative mt-20 py-6">
        <!-- Background with improved visibility -->
        <div class="absolute inset-0 bg-gradient-to-b from-slate-900 via-indigo-900/90 to-black backdrop-blur-xl"></div>
        
        <!-- Content Container -->
        <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <!-- Copyright -->
            <div class="flex justify-center items-center">
                <p class="text-white/60 text-sm text-center">
                    © 2025 YCIS Data Center. All rights reserved.
                </p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script>
        // Mobile menu toggle
        const mobileMenuButton = document.getElementById('mobile-menu-button');
        const mobileMenu = document.getElementById('mobile-menu');

        mobileMenuButton.addEventListener('click', () => {
            mobileMenu.classList.toggle('hidden');
        });

        // Date and Time Update Function
        function updateDateTime() {
            try {
                const now = new Date();
                
                // Format date
                const dateOptions = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
                const dateStr = now.toLocaleDateString('en-US', dateOptions);
                
                // Format time
                const timeOptions = { hour: '2-digit', minute: '2-digit', second: '2-digit', hour12: true };
                const timeStr = now.toLocaleTimeString('en-US', timeOptions);
                
                // Update DOM with null checks
                const dateElement = document.getElementById('currentDate');
                const timeElement = document.getElementById('currentTime');
                
                if (dateElement) dateElement.textContent = dateStr;
                if (timeElement) timeElement.textContent = timeStr;
            } catch (error) {
                console.error('Error updating date/time:', error);
            }
        }

        // Ensure DOM is loaded before initializing
        document.addEventListener('DOMContentLoaded', function() {
            // Update immediately
            updateDateTime();
            // Then update every second
            setInterval(updateDateTime, 1000);
        });
    </script>
    <!-- Vercel Analytics -->
    <script>
        window.va = window.va || function () { (window.vaq = window.vaq || []).push(arguments); };
    </script>
</body>
</html>
<script>
    // Smooth scroll function
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            document.querySelector(this.getAttribute('href')).scrollIntoView({
                behavior: 'smooth'
            });
        });
    });
</script>
</body>

<!-- Add this script before the closing body tag -->
<script>
    function scrollToServices() {
        const servicesSection = document.getElementById('services-section');
        servicesSection.scrollIntoView({ behavior: 'smooth' });
    }
</script>

<script>
    // Initialize AOS with custom settings
    AOS.init({
        duration: 1000,
        once: true,
        offset: 100,
        delay: 100,
        easing: 'ease-out-cubic'
    });

    // Refresh AOS on dynamic content changes
    document.addEventListener('DOMContentLoaded', function() {
        AOS.refresh();
    });

    // Add data-aos attributes dynamically
    document.querySelectorAll('.animate-reveal').forEach((el, index) => {
        el.setAttribute('data-aos', 'fade-up');
        el.setAttribute('data-aos-delay', (index * 100).toString());
    });
</script>
</body>

<!-- Add this script before the closing body tag -->
<script>
    function scrollToServices() {
        const servicesSection = document.getElementById('services-section');
        servicesSection.scrollIntoView({ behavior: 'smooth' });
    }
</script>

<style>
    @keyframes fade-in-down {
        0% {
            opacity: 0;
            transform: translateY(-20px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fade-in-up {
        0% {
            opacity: 0;
            transform: translateY(20px);
        }
        100% {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-in-down {
        animation: fade-in-down 1s ease-out forwards;
    }

    .animate-fade-in-up {
        animation: fade-in-up 1s ease-out forwards;
        animation-delay: 0.3s;
    }

    /* Custom Animations */
    @keyframes reveal {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    @keyframes float {
        0% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
        100% { transform: translateY(0px); }
    }

    @keyframes gradient {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    .animate-reveal {
        opacity: 0;
        animation: reveal 1s ease forwards;
    }

    .animate-float {
        animation: float 3s ease-in-out infinite;
    }

    .animate-gradient {
        animation: gradient 3s linear infinite;
    }

    .animate-pulse-slow {
        animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
    }

    @keyframes slide-up {
        from { opacity: 0; transform: translateY(30px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .animate-slide-up {
        opacity: 0;
        animation: slide-up 1s ease forwards;
    }
</style>